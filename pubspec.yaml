name: flutter_architecture_cli
description: A command-line tool to generate a production-ready clean architecture in Flutter using RxDart and Provider.
version: 1.0.3
repository: https://github.com/Dharti1623/flutter_architecture_cli
environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  args: ^2.5.0
  path: ^1.9.0
  io: ^1.0.4

dev_dependencies:
  lints: ^3.0.0
  test: ^1.25.0

executables:
  flutter_architecture_cli: flutter_architecture_cli
